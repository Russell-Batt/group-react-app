function Section(props) {
    const {title, paragraph} =props

    


    
    return(
        <div>
            <h1>{title}</h1>
            <p>{paragraph}</p>
        </div>
    )
}

export default Section